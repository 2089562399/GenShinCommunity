<UserControl x:Class="原社区.Views.Dialogs.GeographySectionShowDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:原社区.Views.Dialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Image x:Name="GeographySectionShow" Source="{Binding Path}" />
        <Button Height="{Binding ElementName=GeographySectionShow ,Path=Height}" Command="{Binding CloseCommand}"
                FontSize="20" BorderThickness="0" Background="Transparent"
                Width="{Binding ElementName=GeographySectionShow,Path=Width}" Cursor="Hand"/>
    </Grid>
</UserControl>
